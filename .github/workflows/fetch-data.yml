name: GitPulse Data Fetch

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:
    inputs:
      rewrite_ai_roasts:
        description: "Rewrite existing AI roast content with the configured LLM"
        required: false
        default: false
        type: boolean

jobs:
  fetch-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
      models: read
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22

      - run: npm ci

      - name: Fetch GitHub activity data
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.rewrite_ai_roasts == false }}
        run: npm run fetch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY || secrets.GITHUB_TOKEN }}
          LLM_BASE_URL: ${{ vars.LLM_BASE_URL }}
          LLM_MODEL: ${{ vars.LLM_MODEL }}

      - name: Rewrite existing AI roasts
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.rewrite_ai_roasts == true }}
        run: npm run rewrite:ai-roasts
        env:
          LLM_API_KEY: ${{ secrets.LLM_API_KEY || secrets.GITHUB_TOKEN }}
          LLM_BASE_URL: ${{ vars.LLM_BASE_URL }}
          LLM_MODEL: ${{ vars.LLM_MODEL }}

      - name: Commit data changes
        id: commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            git commit -m "chore: update data" && git push
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Trigger deploy
        if: steps.commit.outputs.changed == 'true'
        run: gh workflow run deploy.yml
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
