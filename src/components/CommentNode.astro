---
import "../styles/timeline.css";

interface Props {
  event: {
    type: "issue_comment";
    ts: string;
    repo: string;
    semantic: string | null;
    data: {
      issueNumber: number;
      issueTitle: string;
      body: string;
    };
  };
}

const { event } = Astro.props;
const { data, repo, ts } = event;

const time = new Date(ts).toLocaleString("en-US", {
  month: "short",
  day: "numeric",
  hour: "2-digit",
  minute: "2-digit",
});
---

<div class="event-card event-card--indented">
  <div style="display: flex; align-items: baseline; gap: 6px; flex-wrap: wrap;">
    <span class="event-time" style="color: var(--cp-fg-muted);">comment</span>
    <span class="event-ref">#{data.issueNumber}</span>
    <span style="color: var(--cp-fg-default); font-size: 13px;">{data.issueTitle}</span>
  </div>
  <p class="event-body">{data.body}</p>
  <div style="display: flex; gap: 10px; margin-top: 2px;">
    <span class="event-repo">{repo}</span>
    <span class="event-time">{time}</span>
  </div>
</div>
